{
  "name": "tv-client-manager-web-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node scripts/serve-static.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
